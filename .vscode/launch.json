{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package (Debug config.json)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/src",
            "args": [
                "-config",
                "tests/config.json"
            ],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
